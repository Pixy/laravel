machine:
  php:
    version: 7.0.4
  timezone:
    America/Los_Angeles
dependencies:
  pre:
    - cp .env.example .env
  post:
    - php artisan key:generate
test:
  override:
    - vendor/bin/phpunit --verbose
